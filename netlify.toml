[build]
  base = "frontend/"
  publish = "dist/"
  command = "npm run build"
  
[build.environment]
  NODE_VERSION = "18"

# Optional routing rules for React router (Single Page Application fallback)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
